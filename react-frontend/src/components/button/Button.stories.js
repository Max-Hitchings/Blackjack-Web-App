import React from "react";

import Button from "./Button.jsx";

export default {
  title: "Components/Button",
  component: Button,
};

const Template = (args) => <Button test={"shuhs"} />;

export const Primary = Template.bind({});
